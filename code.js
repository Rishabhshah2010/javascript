var myArr = [1,2,3,4];

for(let a = 0; a < myArr.length; a++)
{
    //console.log(myArr[a]);
    console.log(a);
}
console.log("First for loop is over" + myArr.length)
for(let i = myArr.length-1; i>=0; i--)
{
    //console.log(myArr[i]);
    console.log(i);
}